# 模型权重目录

此目录用于存放模型权重文件。

## YOLO11 模型

YOLO11 模型会在首次运行时自动下载，或者你可以手动放置以下文件：

- `yolo11n.pt` - Nano 版本 (最快)
- `yolo11s.pt` - Small 版本
- `yolo11m.pt` - Medium 版本
- `yolo11l.pt` - Large 版本
- `yolo11x.pt` - XLarge 版本 (最准)

## 自定义情绪识别模型

将自定义的情绪识别模型权重文件放置在此目录，并在配置中指定路径。
